# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp

# Node
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Cloud credentials — should never be committed; the devcontainer keeps these
# in a named volume, not in the workspace.
.aws/

# Build output
dist/
build/
coverage/
*.log

# Astro
.astro/

# Vercel
.vercel/

# pnpm keeps its content-addressable store on the same volume as node_modules;
# in the devcontainer /workspace is a separate mount, so the store lands here.
.pnpm-store/

# Scaffolding handover note — delete when the scaffold is done.
HANDOVER.md

# Brain-dumps: raw material from André, containing company names, internal
# detail and unscrubbed artefacts. Never committed. See docs/WRITING.md §8.
drafts/*
!drafts/README.md

# André's personal AI setup (dotfiles for AI), cloned here for reference only.
# It has its own repo: https://github.com/dr3dr3/dotai
dotai/
